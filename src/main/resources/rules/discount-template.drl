template header
id
ifcondition
thencondition

import org.yuenio.droolSandbox.model.Order;
import org.yuenio.droolSandbox.model.Rule;

template "tmp1"

rule "@{id}"
  dialect "mvel"
  when
    @{ifcondition};
  then
    @{thencondition};
end

end template
